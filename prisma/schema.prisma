generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url = env("DATABASE_URL")
}

// a collection called User with documents id, name, and string

model User{
  id String @id @default(cuid()) @map("_id")
  name String?
  email String @unique
}
